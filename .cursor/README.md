# Cursor project configuration

- **`PROJECT_MAP.md`** — Human-readable map: Tabby root vs `browser-agent-swarm/`.
- **`rules/`** — Cursor rules (`.mdc`): layout, Tabby globs, swarm globs.

Open `PROJECT_MAP.md` first when choosing which part of the repo to edit.
